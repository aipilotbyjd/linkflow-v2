apiVersion: v1
kind: Service
metadata:
  name: linkflow-api
  namespace: linkflow
spec:
  selector:
    app: linkflow-api
  ports:
    - port: 80
      targetPort: 8090
      protocol: TCP
  type: ClusterIP
